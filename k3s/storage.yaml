apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static-pvc
  namespace: gic-chathub
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Mi